import FilterBox from '@/components/filter-box';
import AppLayout from '@/layouts/app-layout';

const Home = () => {
    return (
        <AppLayout>
            <div>
                <FilterBox />
            </div>
        </AppLayout>
    );
};

export default Home;
